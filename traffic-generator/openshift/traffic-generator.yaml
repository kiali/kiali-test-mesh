apiVersion: extensions/v1beta1
kind: ReplicaSet
metadata:
  name: kiali-traffic-generator
  labels:
    app: kiali-traffic-generator
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: kiali-traffic-generator
    spec:
      containers:
      - image: kiali/traffic-generator:latest
        name: kiali-traffic-generator
        imagePullPolicy: Always
        env:
        - name: DURATION
          valueFrom:
            configMapKeyRef:
              name: traffic-generator-config
              key: duration

        - name: ROUTE
          valueFrom:
            configMapKeyRef:
              name: traffic-generator-config
              key: route

        - name: RATE
          valueFrom:
            configMapKeyRef:
              name: traffic-generator-config
              key: rate
